---
hide_body: true
fields:
- type: textarea
  name: content
  label: Content
  config:
    wysiwyg: false
    schema:
      format: html
- type: select
  name: contentType
  config:
    source:
      type: simple
    options:
    - Markdown
    - HTML
  label: Content Type
  default: Markdown
